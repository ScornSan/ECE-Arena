#include "../prototypes.h"
#include "../structures.h"

void direction_fin_tour(t_joueur* j, BITMAP *buffer, int i, int nb_joueurs, int direction)
{
    switch(direction)
    {
       // case 0: // sud ouest
    }
}
